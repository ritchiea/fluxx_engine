%h1

#card-header= @filter_title

#card-body

  = semantic_form_for ModelStub.generate_class(@model_class).new, :url => {:controller => controller.controller_name, :action => 'index'}, :html => {:class => 'to-listing filters-form', :method => 'get'} do |form|
    = render :partial => @filter_template, :locals => { :form => form} if @filter_template
    = form.commit_button 'Create'
    

#card-footer
  %ul.buttons
    %li
      %a.open-filters{:href => '#'} Close
  %ul.workflow
      